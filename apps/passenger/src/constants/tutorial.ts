/** AsyncStorage key marking the in-app coach-mark tour as seen (or skipped) on this device. Written by usePassengerTutorialTrigger on finish()/skip(). */
export const PASSENGER_TUTORIAL_SEEN_KEY = 'trisakay_passenger_tutorial_seen_at';
