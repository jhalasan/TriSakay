import type { NotificationItem } from '../types/notification';

/** Empty pending the backend. The notifications list renders its own empty state. */
export const seedNotifications: NotificationItem[] = [];
