# Driver App — Build Backlog

**Scope:** `apps/driver` only (plus the shared `packages/*` it needs). Originally written 2026-08-03 against a fresh code survey when `apps/driver` was still the stock template; **updated 2026-08-04** after the UI/navigation build (`docs/superpowers/plans/2026-08-03-driver-app-ui.md`, 24 tasks + a final-review fix wave) merged to `main`; **updated again 2026-08-10** after re-checking every item live against the current code — verification-status reading, the availability toggle, the request board, and accept/complete/cancel trip are all real now (items #5–#8 below), and the `match-ride-request`/`paymongo-webhook` Edge Functions this doc previously called missing have both been deployed and verified live since 2026-08-05/07. The database side has been live since before this doc was first written (see `docs/SCHEMA.MD`'s header note) — nothing here is blocked on schema work.

Cross-reference: `docs/CONTEXT.MD` §7 (Driver App feature list), FR-1 (verification), FR-2.5a–e (request board / mid-trip pickup), FR-9.6/9.9 (earnings, cash confirmation), FR-10 (ratings), FR-11 (consent/location). `docs/PASSENGER_TODO.MD` is the sibling doc. `docs/superpowers/specs/2026-08-03-driver-app-ui-design.md` is the design spec the UI build followed.

---

## Where things actually stand (updated 2026-08-10)

**All 14 wireframe screens exist and are navigable end-to-end**, built to match `apps/passenger`'s design system and patterns exactly (Poppins fonts, `packages/ui` components, expo-router structure, per-screen Zustand stores). Typecheck clean, 17 driver-side tests passing. What's real vs. mock, precisely:

**Genuinely real, backend-wired (writes to live Supabase, same project as passenger — `ygdgbvxxqrkxlezpckif`):**
- Registration & login (`app/(auth)/login.tsx`, `register.tsx`) — real `auth.signUp`/`signInWithPassword`, now correctly passing `role: 'driver'` (the old hardcoded-`'passenger'` bug is fixed).
- ToS/Privacy consent gate (`app/consent.tsx`) — writes real rows to `user_consents`, identical mechanism to passenger.
- Location permission gate (`app/location-permission.tsx`) — real `expo-location` foreground permission flow.
- Profile name edit (`app/(tabs)/profile.tsx`) — real `updateProfile()` write to `public.users`, with a `refreshProfile()` call so the Dashboard greeting doesn't go stale after a rename.
- Verification-status gate (`app/verification-pending.tsx`, `useVerificationStore`) — real read of `driver_profiles.verification_status`, gates app entry with pending/rejected-specific copy. **Remaining gap:** no persistent "Verified" indicator once approved — Profile has nowhere that shows it.
- Availability toggle (`useDriverStore.setAvailable`) — real write to `driver_profiles.is_available`/`current_lat`/`current_lng` via `updateDriverAvailability()`, including a one-time GPS fix on going online. The two verification/tricycle trigger rejections are translated into driver-facing copy. **Known bug (undocumented until now):** `isAvailable` initializes to `false` in memory on every app boot and is never read back from `driver_profiles.is_available` — if the app restarts while the driver was online, the backend still shows them available and matchable, but the UI shows "Offline" and never re-subscribes to the request board, so an assigned ride can go unseen. Not yet fixed.
- Request board (`useRequestsStore.subscribe`) — real: calls the `match-ride-request` Edge Function and holds a live Realtime subscription on `ride_requests`, not a client-side timer. (Inherits the bug above — subscribe only fires from the manual toggle handler, never on mount.)
- Accept / complete / cancel trip (`useRequestsStore.accept`, `useTripStore.complete`/`cancel`) — real writes to `trips`/`ride_requests` via `acceptRideRequest()`/`completeTrip()`/`cancelTrip()`.

**Still local/mock — nothing below reads or writes `driver_documents`, `transactions`, `ratings`, `complaints`, or `notifications` yet, and two trip-flow pieces are still local state:**
- `useTripStore.startTrip` — pure local UI state (there's no separate backend "start" step to wire it to; arguably fine as-is).
- `useTripStore.confirmCash` — local toggle only; no `transactions.status='paid'`/`cash_confirmed_by` write yet.
- `useHistoryStore` / `useEarningsStore` — trip history and earnings are local state seeded by completed trips in-session; nothing reads `trips`/`ride_requests`/`v_driver_earnings`.
- `useDocumentsStore` — the Documents & Tricycle screen's upload flow flips a local status flag; nothing is actually uploaded to Storage or written to `driver_documents`/`tricycles`.
- `useComplaintsStore` / `useNotificationsStore` — same pattern, local only.
- Profile's rating display reads `useDriverStore.rating`/`ratingCount`, which nothing ever populates from `driver_profiles.rating_avg`/`ratings`.
- Passenger info on an active trip is still hardcoded `null` (`useTripStore.startTrip` never receives a real name/photo) — no join happens yet.

**Database/backend: ready, still not a blocker.** `driver_profiles`, `tricycles`, `driver_documents`, `trips`, `ride_requests`, `ratings`, `transactions`, `complaints`, `complaint_attachments`, `notifications`, `user_consents`, `account_actions` all exist with RLS already scoped to the `driver` role (`docs/SCHEMA.MD` §7.2–7.8). Triggers already enforce: driver can't go available until verified (`trg_driver_verified_before_available`), location is cleared on going offline (`trg_driver_clear_location`), seat capacity (`trg_ride_requests_seat_cap`).

---

## Build order (backend wiring — the actual remaining work)

Every item below now starts from "screen exists, reads/writes a local store" rather than "screen doesn't exist." The UI-building step from the old version of this table is done for all of them.

| # | Item | Notes |
|---|---|---|
| 4 | ~~Document upload → real Storage + `driver_documents`~~ — **done, `driver_documents` only.** The step-2 upload UI actually lives in `app/(auth)/register.tsx` (the `app/profile/documents.tsx` this row used to name doesn't exist — stale reference, now corrected). `submitDriverDocuments()` (new `packages/services/src/driver-documents/index.ts`) uploads all 4 picked files to a new private `driver-docs` bucket (owner + PSO-read RLS, mirrors `discount-ids`) and inserts one `driver_documents` row per file, with orphan cleanup if a later upload or the insert fails. **Real limitation, not yet solved:** this only runs on the `signed_in` registration outcome — RLS requires an authenticated session, and this project currently has "Confirm email" enabled, so most real registrations return `check_email` (no session yet) and cannot upload immediately. There is no "resume upload after confirming email" screen yet; a driver on that path is told to log in and upload from there, but nothing in the app currently lets them. **No `tricycles` row is created here** — the registration form collects no plate/MTOP fields, and RLS shows a driver *could* write their own tricycle row, but fabricating one with placeholder vehicle data would be wrong; that decision (driver self-reports vehicle info at registration vs. PSO transcribes it during verification, which is admin's still-mock F-item) is unresolved and worth a product decision before building either side. | `packages/services/src/driver-documents/index.ts`, `apps/driver/app/(auth)/register.tsx`, `apps/driver/src/store/useAuthStore.ts`, new `driver-docs` bucket, `driver_documents` |
| 5 | ~~Verification status tracking (FR-1.5–1.6)~~ — **done.** `verification-pending.tsx` + `useVerificationStore` read `driver_profiles.verification_status` for real. **Still open:** no persistent "Verified" indicator on Profile once approved — small follow-up, not tracked as its own line before now. | `driver_profiles` |
| 6 | ~~Availability toggle → real `driver_profiles` write (FR-2.6)~~ — **done.** `setAvailable()` writes real `is_available`/`current_lat`/`current_lng`; trigger rejections surface as driver-facing copy. **Bug found 2026-08-10, not yet fixed:** `isAvailable` never hydrates from the backend on app boot, so a restart while online leaves the driver silently unreachable for new matches (UI shows "Offline", backend still shows available) — see the note in "Where things actually stand" above. | `driver_profiles` |
| 7 | ~~Request Board → real query (FR-2.5a/b, UC20–21)~~ — **done.** `useRequestsStore.subscribe()` calls the live `match-ride-request` Edge Function plus a Realtime subscription on `ride_requests` — no simulated-timer code remains. Inherits item 6's hydration bug (subscription only starts from the toggle handler, never on mount). | `ride_requests`, `trips` |
| 8 | ~~Accept/decline + start trip → real writes (FR-2.5d, FR-2.8, UC22–23)~~ — **mostly done.** Accept (`acceptRideRequest`), complete (`completeTrip`), and cancel (`cancelTrip`) all write real `trips`/`ride_requests` rows. `startTrip()` stays local UI state only (no separate backend "start" step exists to wire it to — likely fine as-is, not a gap). | `trips`, `ride_requests` |
| 9 | ~~Passenger info view~~ — **done.** New `get_trip_passenger_info` security-definer RPC (mirrors `get_trip_driver_info`, reversed — a driver has no direct RLS read on another user's `users` row) + `getTripPassengerInfo()` service wrapper. `dashboard.tsx`'s `handleAccept` fetches it right after accepting and populates it into `useTripStore` via a new `setPassengerInfo()` action; `trip/active.tsx` shows the real name and photo. No contact/messaging (still correctly out of scope per FR-2.9). | `packages/services/src/booking/index.ts`, `apps/driver/app/(tabs)/dashboard.tsx`, `apps/driver/app/trip/active.tsx`, `apps/driver/src/store/useTripStore.ts`, new `get_trip_passenger_info` RPC |
| 10 | **Mid-trip pickup (FR-2.5c).** Surface new compatible requests once a trip is active, subject to remaining seats. Seat-capacity enforcement is already trigger-side (`trg_ride_requests_seat_cap`). No UI for this exists yet at all (out of scope for the wireframe-matching pass) — this is new screen work, not just a wiring swap. | `ride_requests` |
| 11 | ~~Cash confirmation → real write (FR-9.9, UC24)~~ — **done.** `useTripStore.confirmCash(driverId)` now calls `confirmCashPayment()` (new export in `packages/services/src/payments/index.ts`), which UPDATEs the ride's `transactions` row to `status='paid'` with `cash_confirmed_by`/`cash_confirmed_at`, matching the `txn_driver_confirm_cash` RLS policy (update-only, driver-scoped). **Real gap found and fixed as part of this**: nothing previously created the pending cash `transactions` row for that policy to update — added `trg_provision_cash_transaction` (new trigger on `ride_requests`, fires when a cash-method ride is assigned) to provision it, mirroring how GCash's pending row is created by `create-gcash-checkout`. This is scoped to the driver's in-trip "Confirm cash received" toggle only; passenger's `payment.tsx` "Pay cash instead" fallback (a different real-world moment — GCash failed post-completion) is still a `wait()` mock and out of driver scope. | `packages/services/src/payments/index.ts`, `apps/driver/src/store/useTripStore.ts`, `apps/driver/app/trip/active.tsx`, `transactions`, new `trg_provision_cash_transaction` trigger |
| 12 | ~~Trip history → real read~~ — **done.** New `get_driver_trip_history` security-definer RPC (same reasoning as #9 — bulk passenger names across many trips need the same server-side join trick) + `listDriverTripHistory()` service wrapper. `useHistoryStore` now has `load()`/`loading`/`error` instead of a local `addTrip()` mirror; `history.tsx` loads on mount with pull-to-refresh. The local `addTrip()` calls in `active.tsx` after complete/cancel were removed — they'd now double up with the real fetch. | `packages/services/src/booking/index.ts`, `apps/driver/src/store/useHistoryStore.ts`, `apps/driver/app/(tabs)/history.tsx`, `apps/driver/app/trip/active.tsx`, new `get_driver_trip_history` RPC |
| 13 | ~~Earnings → real read (simulated settlement, FR-9.6)~~ — **done, total only.** `getDriverEarnings()` (new export in `packages/services/src/driver-profile/index.ts`) sums `v_driver_earnings.total_collected` for the signed-in driver (a `security_invoker` view — RLS on the underlying `trips`/`transactions` already scopes it, the explicit `.eq('driver_id', …)` is defense-in-depth). `useEarningsStore` gained `load()`/`loading`/`error`; the old local `creditTrip()` accumulator is gone (it would double-count or drift from the real total once the tab reloads), so the `active.tsx` call site was removed too. UI copy untouched — still "tracked", never "payout"/"withdraw". **Not done:** the daily/chart breakdown the view also provides (`earning_date`, `rides_completed`) isn't surfaced — `earnings.tsx`'s "Earnings chart" is still a `MapPlaceholder` graphic, not a real chart; only the single total number is real. `settlementLog`/`notifyPsoForSettlement` remain local-only by design — no `settlements` table exists in the schema for that button to write to. | `packages/services/src/driver-profile/index.ts`, `apps/driver/src/store/useEarningsStore.ts`, `apps/driver/app/(tabs)/earnings.tsx`, `apps/driver/app/trip/active.tsx`, `v_driver_earnings` (view) |
| 14 | ~~View own ratings (FR-10.2–10.3)~~ — **done, summary only.** `getDriverRatingSummary()` (new export in `packages/services/src/driver-profile/index.ts`) reads `driver_profiles.rating_avg`/`rating_count`. Hydrated once at app boot via a new `useRatingSync` hook in `app/_layout.tsx` (mirrors `useAvailabilitySync`'s epoch-guarded pattern exactly) rather than a per-screen fetch, since both Dashboard's stat tile and Profile's `StarRating` read the same `useDriverStore.rating`/`ratingCount`. Profile's existing empty-until-`ratingCount>0` gating logic needed no changes — it was already correct, just fed by dead state before. **Not done:** individual `ratings` rows (e.g. a per-rating comment list) are not read anywhere — only the aggregate `driver_profiles` summary, which is all the current UI displays. | `packages/services/src/driver-profile/index.ts`, `apps/driver/src/store/useDriverStore.ts`, `apps/driver/app/_layout.tsx`, `driver_profiles` |
| 15 | ~~Complaint submission/management → real write (FR-4.1–4.2)~~ — **done, submission only (no "management").** New `packages/services/src/complaints/index.ts` (`submitComplaint`, `listMyComplaints`) writes/reads real `complaints` rows under RLS (`submitted_by = auth.uid()`). `useComplaintsStore` gained `load()`/`submit()`; on a successful submit it re-fetches rather than optimistically guessing the new row (same reasoning as history/earnings dropping their local mirrors). The DB's 6-state `complaint_status` enum collapses to the UI's existing 3-state model (open/review/closed). **Not done:** no category picker in the UI, so `category` is left to the column's default — same already-flagged gap as passenger's complaint form (`docs/CHECKLIST.MD` P2); and no attachment upload UI exists, so `complaint_attachments` has nothing to wire. "Management" (triage/status changes) was never driver-side to begin with — that's PSO's job in `apps/admin`, still mock there. | `packages/services/src/complaints/index.ts`, `apps/driver/src/store/useComplaintsStore.ts`, `apps/driver/app/complaints.tsx`, `complaints` |
| 16 | ~~Notifications → real read (FR-7)~~ — **done.** `packages/services/src/notifications/index.ts` (previously a one-line stub) now has `subscribeToNotifications`/`markAllNotificationsRead`, RLS-scoped to the signed-in user (`notif_own`, `user_id = auth.uid()`, no RPC needed — unlike #9/#12 this is a self-owned table, not a cross-user join). Subscribed for the whole session via a new `useNotificationsSync` hook in `app/_layout.tsx` (not just while the Notifications screen is open), since Dashboard's bell-icon unread badge needs it live too. Nothing sends notification rows yet for `verification_status`/`payment_status` events specifically — this item was only ever about the driver-side *read* path; whether the backend actually produces those rows is a separate, not-yet-verified concern (no Edge Function or trigger currently inserts into `notifications` for either event, as far as this pass checked). | `packages/services/src/notifications/index.ts`, `apps/driver/src/store/useNotificationsStore.ts`, `apps/driver/app/_layout.tsx`, `notifications` |
| 17 | ~~Logout~~ — **done**, real (`app/logout.tsx`). |  |
| 18 | ~~`eas.json` + internal distribution build~~ — **done, driver side only.** New `apps/driver/eas.json`: `development` profile (`developmentClient: true`, internal distribution, Android APK) for local dev-client testing, and `preview` profile (internal distribution, Android APK, iOS `distribution: "store"` for TestFlight upload) matching CONTEXT.MD's "Android internal distribution + iOS TestFlight, no public listing" plan. Root `.gitignore` gained a `credentials.json` entry (EAS's local credential-manager output, never meant to be committed). **Not done / needs the account owner:** no `extra.eas.projectId` in `app.json` — that only gets written by running `eas init` under a logged-in Expo account, which this session has no credentials for; first real `eas build` will prompt for it interactively. Also not verified against EAS CLI's live schema check (`eas config`) since that also requires `eas login` — only validated as syntactically-correct JSON matching the documented profile shape. Passenger's equivalent `eas.json` was intentionally left untouched (out of driver-only scope this session) — `docs/CHECKLIST.MD` P5 still tracks it as its own item. | config |

---

## Open decisions / cross-app blockers (flagged, not resolved)

1. ~~`match-ride-request` Edge Function does not exist anywhere in the repo yet~~ — **resolved.** Deployed and live since 2026-08-05 (`supabase/functions/match-ride-request/index.ts`), implementing the FR-2.5 cluster-authorization hard filter + seat capacity, with the bearing/detour soft filter falling back to hard-filter-only until driver GPS is populated (now is, via item #6). Request Board (#7) is wired to it for real; Mid-trip pickup (#10) still needs its own screen built.
2. ~~`gcash-webhook` Edge Function also doesn't exist~~ — **resolved.** Renamed `paymongo-webhook`, deployed alongside `create-gcash-checkout`, and verified end-to-end live (real PayMongo test-mode GCash payment, webhook delivery confirmed) on 2026-08-07. Driver-side exposure is still limited to cash confirmation (#11), which remains open.
3. **Map reuse for the Request Board.** Passenger's `OsmMap` (Leaflet/WebView) can likely be reused as-is for a driver map view, but rendering multiple pending-request pins on a board is new UI, not just wiring — don't assume it's a drop-in. `app/trip/active.tsx` already reuses `OsmMap` for the single-trip route view.
4. **MapLibre vs. Leaflet/WebView divergence** (same one `docs/PASSENGER_TODO.MD` #1 flags for CONTEXT §3) applies identically here.
5. **New: driver test accounts require manual email confirmation right now.** The live Supabase project has "Confirm email" enabled and a low built-in email-send rate limit, so creating test driver accounts via the Register screen is currently friction-prone (confirmation email required, rate limit hit easily during repeated testing). Either disable "Confirm email" in the dashboard for dev convenience, or use the `service_role` key for one-off pre-confirmed test-account creation. Not a blocker for further development, but worth resolving before QA/demo prep.
