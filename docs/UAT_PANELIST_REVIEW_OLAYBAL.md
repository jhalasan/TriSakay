# UAT Panelist Review — Ranylene O. Olaybal

Source: `UAT-Panelist.pdf` (TriSakay UAT Test document, by Catuiran/Curva/Halasan/San Diego).
This file transcribes every recommendation from the panelist's review, organized by app/section, so each item can be triaged and addressed individually. Status column to be filled in as we work through them.

Legend for **Status**: `Open` / `Agree` / `Disagree` / `Won't Fix` / `Done`
Legend for **Verified**: `EXISTS` (already addressed) / `PARTIAL` (exists but gap remains) / `MISSING` (not implemented)

---

## Passenger App

| # | Screen | Recommendation | Verified | Notes |
|---|--------|-----------------|--------|-------|
| P1 | Splash Page | Consider adding a loading indicator when the app takes time to load or connect. | **DONE** | `app/splash.tsx` — animated `LoadingBar` already existed (unchanged). **Real hang fixed**: `resolveActiveRideRoute()`'s two calls (`getActiveRideForPassenger`, `getTripDriverInfo`) had no timeout — unlike every other network call in this app, a genuinely hung connection here (not an outright error, already caught) stalled the splash screen indefinitely with no fallback. Both now wrapped in the app's standard `withTimeout`/`REQUEST_TIMEOUT_MS` (10s), degrading a hang into the existing "no active ride" path instead of hanging forever. No separate "slow connection" message was added — the existing loading bar already covers that, and this fix ensures it can't spin forever. |
| P2 | Onboarding / Welcome | The onboarding and welcome screens seem repetitive. Consider simplifying these. If the user skips the tutorial, provide an option to view it again later. | **DONE** | `app/walkthrough.tsx` now accepts a `?replay=1` param (`finish()` returns to the caller via `router.back()` instead of the first-launch `/landing` redirect); a new "Replay welcome walkthrough" Settings row pushes it that way. Also fixed the existing "Replay tour" row's subtitle, which claimed to "take the welcome walkthrough again" while actually restarting the unrelated in-app coach tutorial (P7) — now describes what it actually does. |
| P3 | Log In Page | Show appropriate messages for incorrect login details and connection errors. Also check how the system handles repeated failed login attempts. | **PARTIAL → connection-vs-credentials distinction added** | `packages/services/src/auth/index.ts` `signIn()` now checks `error.name === 'AuthRetryableFetchError'` (supabase-js's own network-failure error class, as opposed to `AuthApiError` for a real credential rejection) and surfaces a distinct "Couldn't reach the server" message — shared by passenger, driver, and admin login alike. 2 new service tests. **Repeated-failed-attempt lockout**: scoped out, same reasoning as A1 — Supabase Auth (GoTrue) already rate-limits by IP server-side; a client-visible attempt counter would itself be a security regression. |
| P4 | Create Account Page (Step 1) | Indicate which fields are required and validate the email, mobile number, and password before registration. | **DONE** | Mobile format validated (`isValidLocalMobile`), required fields marked with a red asterisk via a new `TextField` `required` prop, and duplicate mobile numbers are now rejected DB-side. `users_contact_no_unique` constraint **confirmed live** on production (`pg_constraint` verified 2026-09-21) — required first cleaning up 11 pre-existing duplicate/test accounts blocking the constraint (see memory `supabase-project` for the full list and the `account_actions.performed_by` gotcha hit along the way). |
| P5 | Terms of Service & Privacy Policy (Step 2) | Allow the user to read the complete Terms of Service and Privacy Policy before creating an account. Review also whether these should have separate acknowledgment/acceptance. | **DONE** | `app/(auth)/register.tsx` — full versioned ToS/Privacy text is scrollable and readable (unchanged). **Separate acceptance added**: two checkboxes now gate account creation instead of one — the backend already recorded them as two independent `user_consents` rows (`recordConsent()` always inserts both atomically, regardless of UI), so this was purely a client-side gap. |
| P6 | Permissions – Notification/Location | Explain why location and notification permissions are needed. Check what happens when the user denies a permission. | **PARTIAL → notification pre-prompt added** | `app/location-permission.tsx`, `useLocationPermission.ts` — location has a full explanatory modal + denied/blocked handling with "Open settings." **Notifications**: `usePushNotificationsSync.ts` now shows a plain confirm (`Alert.alert`, "Enable notifications?" + why) before the very first `requestPermissionsAsync()` call (`status === 'undetermined'`), declining if the rider taps "Not now" — a lighter-weight fix than location's full modal/store, since this hook runs silently in the background on every app load rather than from a dedicated screen. Denial (either at this prompt or the OS dialog) is still swallowed silently, same fail-open discipline as before. |
| P7 | First Time User Tutorial (Coach) | No recommendation. | N/A | |
| P8 | Home Page | No recommendation. | N/A | |
| P9 | Booking Request | Allow the passenger to check or adjust the detected pickup point before confirming the ride, especially if the GPS location is inaccurate. | EXISTS | `app/booking/request.tsx:35-63`, `app/booking/set-pickup.tsx` — draggable pin, search, and "use current location" all available; already addresses this fully. |
| P10 | Choose Destination | Make the pickup and destination markers easier to distinguish. Show enough location details before the passenger confirms the booking. | **DONE** | Original audit claim was slightly wrong (the live map already distinguished them), but investigating turned up a real, larger app-wide inconsistency: pickup/dropoff colors conflicted across `mapHtml.ts`, `set-destination.tsx`, `trip.tsx` (both apps), and `trip-complete.styles.ts` — some screens used green=pickup/blue=dropoff, others the reverse. Standardized the whole app (passenger + driver) on green=pickup, blue=dropoff, matching the home screen and ride-history conventions. Location details were already shown before confirming. |
| P11 | Confirm Ride/Fare Estimate | How is the estimated fare computed? Show the basis of the fare and clarify when the final fare may be different from the estimate. | EXISTS | `app/booking/confirm.tsx:283-328` — links to fare matrix, shows distance, and explicit "final fare confirmed at drop-off" copy. |
| P12 | Finding A Driver | What happens if no driver accepts the request? Provide a timeout and an option to retry or cancel. | EXISTS | `app/booking/finding-driver.tsx:21,57-61` — 60s timeout routes to `no-drivers-nearby.tsx` with retry/change-pickup/notify options, plus explicit Cancel while searching. |
| P13 | Driver Assigned | Consider providing a way for the passenger and driver to communicate without exposing their personal contact numbers. | POLICY ANSWER, NOT A GAP + future plan noted | `app/booking/trip.tsx:273` — deliberately shows "No in-app call or message — coordination is in person." **2026-09-21 resolution**: full free-form chat/calling is explicitly out of scope for this project — a passenger and driver never need each other's number because live location, ETA, driver name/plate, and the ride details already answer "where is my ride," which is the only coordination question a 5-10 minute pickup window actually has. Reached the same way A14 did: not an unsolved gap, a deliberate scope boundary matching the project's size. **If a real gap surfaces later** (a genuine "I can't find them" case beyond what live tracking covers), the recommended direction is **preset quick-messages**, not free-text chat: a small fixed set of one-tap templates ("On my way", "Running a few minutes late", "I'm at the pickup point", "Can't find you, where are you?") sent as a new `notifications` row (reusing the existing `notification_type` enum + table rather than a new messaging subsystem/realtime chat). No exposed numbers, no moderation/abuse surface since messages aren't free text, and a much smaller build than a chat feature or a masked-calling telephony integration (which would need a third-party provider and ongoing per-message cost). Not built — documented here as the shape a future fix should take if this is ever revisited. |
| P14 | Driver Assigned | Clarify who receives the SOS alert and what information is sent. Also test what happens when the internet connection is weak or unavailable. | EXISTS | `app/booking/emergency.tsx:35-53,90-105,107-132` — shows exactly what's shared with PSO (driver name/plate, live location, destination) and has an explicit `'failed'` retry state for connectivity issues. **2026-09-21**: that failed-state now actually triggers reliably on a hung connection too, not just an explicit rejection (see D9 fix — `withTimeout` added). |
| P15 | Trip Complete | Add a trip/reference number. This can be used for complaints or transaction verification. | **DONE** | Added `src/utils/reference.ts` (6-char uppercase UUID-tail code). Now shown on Trip Complete, Ride History detail, and wired functionally into the Complaints related-trip picker (so the reference is what distinguishes trips when filing a complaint, not just decorative). |
| P16 | Rate Driver | Consider adding common feedback categories aside from the comment box. | **DONE** | `app/booking/rate-driver.tsx` — 8 tappable category chips (Friendly, Safe driving, Clean vehicle, On time, Late, Rude, Unsafe driving, Poor vehicle condition) alongside the existing star rating + free-text comment. Discussed and agreed: stored as a real structured column, not folded into the comment text, so it stays queryable/reportable — new `ratings.tags rating_tag[]` (`supabase/migrations/20260921000004_add_rating_tags.sql`, fixed 8-value enum, **applied live 2026-09-21**). No RLS change needed (`ratings_passenger_insert` already checks only `passenger_id = auth.uid()`, no per-column restriction). Also threaded through `listMyRatingsAsDriver()` or the driver app's own ratings store, though nothing in that app's UI renders individual rating rows yet (pre-existing gap, not created by this fix). 2 new service tests. |
| P17 | Ride History | Allow the passenger to open a previous trip and view its complete details. | EXISTS | `app/history/[id].tsx` — full detail view already exists (route, fare breakdown, driver, payment status, cancellation reason). |
| P18 | Complaints | Add a complaint/reference number and show the status of the complaint so the passenger can track it. | EXISTS | `app/complaints/[id].tsx:125-141` — reference number shown, 3-stage status tracker (Received → Under Review → Resolution) already implemented. |
| P19 | Profile Page | Avoid displaying complete personal information when it is not necessary. Require verification when changing important account information such as email or mobile number. | **PARTIAL → password re-verification added for phone changes** | `app/(tabs)/profile.tsx` — full email/phone still shown unmasked (unchanged — a passenger's own profile, not a gap). Email isn't editable here at all (unchanged). **Phone-change gate added**: saving a changed phone number now requires re-entering the current password first (`verifyCurrentPassword`, the same re-auth pattern already used for admin password changes), via a new confirm modal — closes "an unattended, unlocked device could silently swap the number" without needing SMS OTP infra, which doesn't exist anywhere in this codebase. **True OTP (SMS or email-confirmation-link) remains deferred** — would need a real SMS provider integration or piggybacking on Supabase Auth's email-change confirmation flow, out of scope for this pass. |
| P20 | Settings Page | Provide account-management options such as logout and account deletion/deactivation, and clearly explain the effect of disabling location tracking. | **DONE** | **Location tracking**: the toggle now gates the silent GPS auto-detect-on-open on the Request a Tricycle screen (`app/booking/request.tsx`) — turning it off means the app no longer grabs the rider's location the instant the screen opens; they search or drop a pin instead. The explicit "use current location" button stays available either way (a one-time action the rider directly asked for, not automatic tracking), and SOS location capture is deliberately never gated by this toggle (safety-critical). **Account deactivation**: new self-service flow — `/deactivate-account` routed `ConfirmModal` (same pattern as `/logout`) calls a new `self_deactivate_account()` Postgres RPC (`supabase/migrations/20260921000002_add_self_deactivate_account_rpc.sql`, **applied live 2026-09-21**), which can only ever set the *caller's own* row to `account_status='deactivated'` and only from `active` status, writing the required `account_actions` audit row (`performed_by = target_user_id`, distinguishing it from a PSO-performed deactivation). The app already had full support for the resulting state (`app/account-suspended.tsx`'s existing `deactivatedTitle`/`deactivatedBody` copy, root-layout gating) from an earlier PSO-initiated-only flow — no other client-side plumbing was needed. 2 new service tests (305 total). Typecheck/build/tests clean across passenger (27), driver (76), admin (71), and services. |
| P21 | Privacy Center | Clarify the SOS process. Who receives the alert, what information is sent, and what happens if there is no internet connection? This should be demonstrated during testing. | EXISTS | `app/profile/privacy-safety.tsx:39-72,151-159` — dedicated "How SOS works" card, live sending/sent/failed states, explicit failure fallback message ("Could not reach PSO... call 911 directly"). **2026-09-21**: same `withTimeout` fix as D9 applied here too — the failure message now reliably appears on a hung connection, not just an explicit rejection. |
| P22 | Manage Saved Places | Provide an edit option in addition to delete, and require confirmation before deleting a saved location. | **DONE** | Delete confirmation via `Alert.alert` already existed. **Edit added**: new `updateSavedPlace(id, {label, icon})` service (RLS `saved_places_own` already covers `for all`, no migration needed) + a pencil-icon row in `manage.tsx` that opens the existing `SavePlaceSheet` in edit mode (new `editingId`/`initialLabel`/`initialIcon` props — label/icon change in place, address/coordinates untouched). 1 new service test. |

---

## Driver App

| # | Screen | Recommendation | Verified | Notes |
|---|--------|-----------------|--------|-------|
| D1 | Driver Log in Page | Show appropriate messages for incorrect login details and connection errors. Check also how repeated failed login attempts are handled. | **PARTIAL → connection-vs-credentials distinction added** | Same shared `signIn()` fix as P3 — see that row. Lockout scoped out for the same reason as A1/P3. |
| D2 | Register as Driver | Check for duplicate email addresses and mobile numbers. Clearly indicate all required fields. | **DONE** | Same fix as P4, shared via `packages/services` `signUp()`: mobile format validated, required fields (incl. plate number) marked with asterisks, duplicate mobile numbers rejected DB-side with a friendly error. Duplicate **email** was already handled natively by Supabase Auth before this fix. |
| D3 | Document Submissions | Indicate the required documents, accepted file types, and file-size limit. Make sure uploaded licenses, OR/CR, permits, and other documents are accessible only to authorized personnel. | **PARTIAL → requirements copy added** | `app/(auth)/register.tsx`'s existing `documentsIntro` line now states all 4 documents are required and that a clear photo per document is accepted, from the gallery — no verified/enforced numeric size limit exists anywhere in this codebase to state one truthfully, so that specific ask was left out rather than guessed. Access control **is already solid** (unchanged from prior audit): `driver-docs` is a private bucket with owner-scoped RLS delete policy + RPC-only insert path. |
| D4 | Under Review | Typo/correction: "the account is **not under** review" should read "the account is **now under** review by the PSO Personnel…" | EXISTS (doc typo) | `en.ts:758-762`, `app/verification-pending.tsx:163-166` — current copy already reads "Your account is under review... You'll get a text message or email once approved." Does **not** say "not under review" — the panelist's quoted phrase doesn't match current/likely original code; appears to be an error in the UAT document itself, not the app. |
| D5 | Under Review | Provide the reason when an application or document is rejected and allow the driver to correct and resubmit the affected document. Include status and notification of the verification result. | **DONE** | `getDriverVerificationStatus()` fetches `driver_documents.remarks` when rejected, surfaced via `useVerificationStore.rejectionReason`. **2026-09-21**: `submit_driver_documents`'s live body was pulled via `execute_sql` and reviewed — its `on conflict ... do update` on both `tricycles` and `driver_documents` resets status to `pending` and clears the old reviewer/remarks fields on the existing rows rather than creating duplicates, so calling it a second time is genuinely safe. `verification-pending.tsx`'s upload form (previously only for `unsubmitted` drivers) is now reused for `rejected` drivers too, with the rejection reason shown above the form. |
| D6 | Driver Home Page (Online/Offline Toggle) | Check if the driver's location is really no longer tracked when the driver goes offline. | EXISTS | `useDriverLocationSync.ts:63-71` — `shouldRun()` requires `isAvailable === true`; going offline calls `stop()` which removes the `watchPositionAsync` subscription immediately. Confirmed working as intended. |
| D7 | Ride Request Board | Show the necessary trip details before the driver accepts the request. You may also add a time limit for accepting the request. | **DONE** | `RequestCard.tsx` shows pickup/dropoff/distance/fare/seats/payment (unchanged, already good detail). **Countdown added to the Requests board**: `RequestCard` gained an optional `countdownSeconds` prop rendering the same chip style as Dashboard's; `requests.tsx` (which lists *every* pending request, unlike Dashboard's single `incoming` one) wraps each row in a new local `RequestCardWithCountdown` component so each card independently calls `useRequestCountdown(item.expiresAt)`. |
| D8 | Trip Started | Prevent accidental cancellation/completion through confirmation prompts. Clearly define when a trip may be marked complete and how disputes involving premature completion are handled. | **DONE** | `app/trip/active.tsx` — the per-passenger "Complete" button now opens a `ConfirmModal` (same pattern as Cancel/End Trip) before calling through to the completion logic, instead of firing on a single tap. "When a trip may be marked complete" is already answered by D10 (`canComplete` gated on cash confirmation for cash payments); dispute handling for premature completion is a policy/process question outside this codebase's scope. |
| D9 | Trip Started | Test SOS under poor/no connectivity and specify the fallback behavior. The application should clearly confirm whether an emergency alert was successfully transmitted. | **DONE** | All 4 `triggerEmergencyAlert` call sites (driver `trip/emergency.tsx` + `profile/privacy-safety.tsx`, passenger `booking/emergency.tsx` + `profile/privacy-safety.tsx`) now wrapped in `withTimeout` (10s, matching the app-wide standard) — a hung connection now reliably surfaces the existing `'failed'` UI state instead of hanging indefinitely. All were already in try/catch, so the timeout's rejection is caught with no other logic changes needed. Still no SMS/other out-of-band fallback — that would be new functionality, out of scope for this fix. |
| D10 | Trip Started | Review the sequence of "Confirm cash received" and "Complete." The system should prevent inconsistent transaction states and maintain an auditable transaction record. | EXISTS | `app/trip/active.tsx:221-223,251-260` — `canComplete` is gated on `cashConfirmed` for cash payments, so Complete cannot fire before cash confirmation client-side. (No confirmation dialog on Complete itself — see D8.) |
| D11 | Trip History | Allow the driver to open individual trips and view transaction details and reference numbers for reconciliation or disputes. | **DONE (scoped to available data)** | New `app/history/[id].tsx` detail screen (mirrors the passenger app's equivalent), tap-to-open wired up from the list. Shows passenger, date/time, fare, status, and a reference number (same `getReferenceCode` pattern as the passenger app). **Scope note**: the underlying `get_driver_trip_history` RPC only returns passenger name/status/fare/date — no pickup/dropoff/distance/payment method, unlike the passenger app's richer history detail. That RPC is also untracked in this repo's migrations (same dashboard-only-function issue as D5/A13), so extending it to return more was deferred rather than guessed at blind. |
| D12 | Earnings/Peak Hours Analytics | Clearly label the period covered and explain how earnings and busiest-hour analytics are calculated. Avoid presenting incomplete data as definitive analytics. | EXISTS | **Re-verified 2026-09-21, found already fixed** (not by this session — the code no longer matches the original audit note): `app/(tabs)/earnings.tsx` — "Busiest hours (**last 30 days**)" section label plus a caption "Based on completed rides city-wide — use it to decide when to go online." The all-zero-sample `EmptyState` guard from the original audit is still the only "incomplete data" handling, which is a lighter bar than a low-but-nonzero-sample warning, but the panelist's two literal asks (period + methodology) are both now met. |
| D13 | Driver Profile Page | Allow the driver to review verification status and document validity/expiration where applicable, with appropriate notification before documents expire. | **DONE** | `app/(tabs)/profile.tsx` still shows `mtopExpiryDate` as static text (unchanged). **"My Documents" screen** (`app/profile/documents.tsx`, linked from Profile's Account section): lists all 4 document types with a driver-editable expiry date (`driver_documents.expiry_date` — `supabase/migrations/20260921000005_add_driver_document_expiry.sql`, **applied live 2026-09-21**) and a client-computed status badge (Expired / Expiring soon within 30 days / Valid / Not set). **2026-09-21 — proactive push notification built**: new `notify-expiring-documents` Edge Function (deployed live, `verify_jwt=false`, same shared-secret model as `notify-drivers-new-request`) queries documents expiring within 30 days and pushes to the driver's stored `push_token` via Expo; invoked daily by a new `pg_cron` job (`notify-expiring-driver-documents-daily`, `supabase/migrations/20260921000007_add_document_expiry_notifications.sql`, **applied live**) since "30 days before expiry_date" has no DB event to trigger off, unlike the row-insert trigger the new-ride-request push uses. Idempotent via a new `driver_documents.expiry_notified_at` column, reset to null whenever the driver edits the date so a renewal becomes eligible again. Both pg_cron/pg_net were already enabled on this project. 3 new service tests. |

---

## Admin Web Portal (PSO Operations)

> Context: `docs/ADMIN_TODO.MD` shows a "2026-09-15 launch audit" already hardened much of this portal's RLS/RPC layer (`supabase/migrations/20260915000002_harden_rls_p1_findings.sql` and siblings). Several items the panelist likely observed as frontend-only mocks are now real, backend-enforced features. Gaps that remain are called out concretely below.

| # | Screen | Recommendation | Verified | Notes |
|---|--------|-----------------|--------|-------|
| A1 | Admin Log in | Implement appropriate authentication security, failed-login controls, session timeout, and audit logging for administrative accounts. | **DONE (3 sub-items resolved as scoped)** | `Login.tsx`, `useSessionStore.ts` — real Supabase Auth sign-in, rejects non-PSO roles, error banner shown. **Session idle timeout**: new `useIdleTimeout` hook mounted in `AppShell.tsx` — 15 min of no mouse/keyboard/touch/scroll activity signs the session out (a 2-min-prior warning toast fires first), redirecting to `/login` with an inline "signed out due to inactivity" notice. **Login/logout audit trail**: new `login_events` table (`supabase/migrations/20260921000003_add_login_events.sql`, **applied live 2026-09-21**) — self-reported by each client at the moment of `signIn()`/`signOut()` (fire-and-forget, self-scoped insert RLS so a client can never write another user's row), surfaced as a new "Login Activity" table on the Audit Log screen alongside the existing Account Actions and Verification/Discount Decisions tables. Caveat: this is a client-reported log, not a server-verified session record — a client could in principle omit its own logout event, same trust model as every other client-self-reported table in this app. **Failed-login lockout**: discussed and scoped down — Supabase Auth (GoTrue) already applies IP-based sign-in rate limiting server-side as a baseline; a client-visible "failed attempts" counter queryable before authentication would itself be a security regression (it lets an attacker enumerate which emails have accounts). True per-account lockout would need a dedicated backend design session, not a quick client fix — documented here as the accepted scope boundary rather than built. 6 new service tests (311 total), 2 new admin tests (73 total). |
| A2 | Admin Dashboard (Administrator) | Ensure dashboard statistics are traceable to underlying records and clearly indicate the reporting period and last data update. | **DONE** | `Dashboard.tsx` — real counts from `users`/`trips`/`driver_profiles`/`complaints`; "Needs attention" cards click through to underlying queues (genuinely traceable, unchanged). Charts labeled "Last 7 days" (unchanged). **Gap closed**: a `lastLoadedAt` timestamp is now stamped when the page's `Promise.all` load settles and shown as "· As of {relative time}" next to the existing "Needs attention today" date/queue-count line. |
| A3 | Ride Monitoring | Provide filtering/search by date, trip status, passenger, driver, and reference number. Clearly distinguish active, completed, cancelled, and emergency-related rides. | **DONE (new screen)** | Discussed and agreed: Ride Monitoring itself is intentionally live-only (FR-5.1 wireframe scope, coarse real-time tricycle positions) and Reports' Transactions table only covers *paid* rides — neither can represent cancelled/in-progress rides. Built a new **Ride Log** screen (`/ride-log`, sidebar under Operations) backed by a new `listRideLogForAdmin()` reading `ride_requests` directly (PSO already has full RLS read access — no new migration needed, avoiding the untracked-RPC problem hit elsewhere). Filters: date range, status (pending/assigned/ongoing/completed/cancelled), and free-text search by passenger/driver/reference number; each row shows an SOS badge when linked to an `emergency_alerts` row. Typecheck, build, and 4 new service tests (301 total) all pass. **2026-09-21**: the sidebar's `/ride-log` entry was missing from `NAV_ICONS` (`Sidebar/navIcons.tsx`), so it silently rendered with no icon — added a 16px stroked "history" icon (circular arrow + clock hands) matching the rest of the set. |
| A4 | Emergency Alerts | Provide acknowledgement and incident-status workflow (New → Acknowledged → Responding → Resolved), timestamps, responsible personnel, passenger/driver information, location, and an audit trail of actions taken. | **DONE (as a scoped decision)** | The full 4-stage dispatch-style workflow the panelist described conflicts with this feature's documented design intent — the schema comment and FR-12.7 explicitly state this is a lightweight incident *log* for accountability, not an actively-staffed dispatch pipeline ("isn't meant to be 24/7-monitored"). Discussed with the user and resolved as: keep the existing `logged → reviewed` flow, but wire up the third enum value, `closed`, which previously had zero UI path despite existing in the schema since 2026-08-21 (documented then as "revisit only if a real product need... shows up" — this UAT review is that need). Added `markEmergencyAlertClosed()` (same Supervisor+ RLS tier as review) and a "Close" button in the admin modal, enabled only once an alert is `reviewed`. Existing timestamps/personnel/location/notes fields already satisfied the rest of the ask. |
| A5 | Driver Management | Administrative actions such as suspension, reactivation, or rejection should require confirmation and a documented reason. Maintain an audit log indicating who performed the action and when. | EXISTS | `Drivers.tsx` — Suspend/Reactivate/Flag route through `ConfirmModal` requiring non-empty reason, then `perform_account_action()` RPC atomically writes `account_actions` (who/when/reason) + updates status. One of the most solidly implemented items. |
| A6 | Driver Detail Modal | (Panelist left this blank in source doc.) | N/A | Modal exists (`Drivers.tsx` "View" modal) with grouped detail sections; nothing specific to evaluate since no recommendation was given. |
| A7 | Passenger Management | Restrict access to personal data based on administrative role and avoid displaying unnecessary personal information. | **DONE** | Contact number and email are hidden from PSO Staff in the table, detail modal, and CSV export — visible only to Supervisor+, matching the existing write-action tier boundary. Name/rides/discount/status stay visible to Staff (needed for FR-4.3 complaint triage). **2026-09-21 — real backend closure**: new `admin_passenger_directory` view (`security_invoker = true`, same pattern as `v_driver_earnings`/`v_flagged_low_ratings`) masks `contact_no`/`email` to `null` at the database level for any caller that isn't `is_supervisor()`, closing the earlier UI-only gap (Postgres RLS is row-level, not column-level — a technically-inclined Staff user could previously fetch the full row directly via their own session). `listPassengersForAdmin()` now reads this view instead of `users` directly. `supabase/migrations/20260921000010_add_passenger_directory_masking_view.sql`, **applied live 2026-09-21**. |
| A8 | Tricycle Management | Provide status and verification information for each registered vehicle and prevent duplicate plate/franchise records. | EXISTS | `Tricycles.tsx`, `SCHEMA.MD:243` — `plate_no` has a DB-level `unique` constraint; screen shows verification status + MTOP-expiry badges per vehicle. |
| A9 | Driver & Tricycle Verification | Record who verified or rejected each application, date/time of action, reason for rejection, and supporting verification remarks. Verification decisions should be auditable. | EXISTS | `DriverVerification.tsx`, `perform_verification_decision()` RPC — writes `verified_by`/`reviewed_by`, timestamps, shared `remarks` atomically; Reject requires a note; decisions surface in Audit Log's "Verification & Discount Decisions" table. |
| A10 | Driver & Tricycle Verification Modal | (Panelist left this blank in source doc.) | N/A | Modal exists (`DriverVerification.tsx` case panel); nothing specific to evaluate. |
| A11 | Fare Discount Request Management | Define the approval criteria and validity period for Senior Citizen, PWD, and student discounts. Rejected applications should include a reason and resubmission mechanism. | **DONE (validity period)** | `DiscountReview.tsx` — Reject requires remarks (unchanged). **Validity period added**: new `passenger_discounts.expires_at` column (`supabase/migrations/20260921000006_add_discount_expiry.sql`, **applied live 2026-09-21**), set to reviewed_at + 1 year on approval (decided policy default). `compute_fare()` — a tracked, migration-backed function, safe to modify directly — now checks `status = 'approved' and (expires_at is null or expires_at > now())` instead of status alone, so an expired discount genuinely stops reducing fares even though nothing flips its DB status automatically (no scheduler exists). **Real conflict found and resolved**: `passenger_discounts_one_live_claim` (a unique index) blocks a second pending/approved row per passenger, so an expired-but-still-`approved` row would otherwise trap a passenger from ever reapplying. Resolved by discussion: PSO can now click Reject on an expired approved row (previously locked once decided) to clear it, explicitly labeled "Reject (reset expired)" with copy explaining why, so the passenger can submit a fresh application. Passenger-facing discount banner (`app/(tabs)/profile.tsx`) shows "valid until {date}" and `getPassengerStats()`/`compute_fare()` treat an expired discount identically (both check expiry, not just status). 4 new service tests. **Approval-criteria fields remain undefined** — no ordinance-derived rule set exists to encode beyond the 1-year default; that's a policy document, not a schema gap. |
| A12 | Complaint Management | Implement ticket/reference numbers, status tracking, assignment to responsible personnel, timestamps, resolution notes, and complete action history. | **DONE** | `Complaints.tsx` — 6-state status enum, SLA/business-days-elapsed badges, DH directive field, mediation scheduling (Supervisor+), full evidence display (unchanged). **Gap closed**: the modal subtitle now shows "Ref. #XXXX" using the same 4-char `getReferenceCode(id, 4)` derivation the passenger app's own complaint detail screen already uses — a rider quoting their reference number over the phone now maps to the same complaint PSO sees. |
| A13 | Complaint Management Modal | Provide a complete complaint action history showing status changes, assigned personnel, timestamps, remarks, and resolution details. Require a reason or resolution note before a complaint can be marked as resolved or closed. | **DONE** | `Complaints.tsx` — "Save Outcome" `disabled` until `resolutionNotesDraft.trim()` is non-empty. **2026-09-21 — server-side hardened too**: `record_complaint_resolution`'s live body was pulled via `execute_sql` (it had no non-empty check server-side, confirming the gap was real, not just theoretical) and replaced via `CREATE OR REPLACE` with an added `if p_notes is null or length(trim(p_notes)) = 0 then raise exception` check, reproducing its exact prior behavior otherwise. `supabase/migrations/20260921000008_harden_complaint_resolution_notes.sql`, **applied live**. Action history is now covered by A16's new status-history timeline. |
| A14 | Rating Oversight Management | Provide filtering and review mechanisms for unusually low ratings and reported rating abuse. If administrators are allowed to modify, hide, or invalidate ratings, require a documented reason and maintain an audit trail of the action. Panelist question: *"Can the administrator delete or change a passenger's rating? If yes, who is authorized to do it, and is the action recorded?"* | POLICY ANSWER, NOT A GAP | `RatingOversight.tsx` is read-only by design; `ratings` RLS (`SCHEMA.MD:1691-1698`) has **no update/delete policy for anyone, including admins** — so the direct answer to the panelist's question is "no, nobody can, by design." This is a clean, auditable answer but should be **explicitly documented** as a stated policy rather than left implicit for a reviewer to discover. |
| A15 | Reports & Analytics | Clearly indicate the reporting period, data source, and meaning of each metric. Provide appropriate filters and ensure that displayed statistics can be traced to the underlying transaction records. If reports can be exported, apply appropriate access controls and data-minimization measures. Panelist question: *"If the report says there were 150 completed rides this month, can you demonstrate where those 150 records came from?"* | **DONE** | `Reports.tsx` — explicit resolved date range shown, stats tie directly to the Transactions table rendered beneath (traceable), CSV export works — answers the panelist's literal question well (unchanged). **Access control added**: discussed and agreed — on-screen viewing stays open to every PSO role (consistent with A7's precedent that names aren't restricted for Staff), but the CSV export button is now gated to Supervisor+ (`meetsRoleGate(role, 'supervisor')`), since bulk exfiltration of identifiable names is a distinct risk from the same names being visible on-screen. |
| A16 | Audit Logs | Ensure that audit logs capture the user/account, action performed, affected record, date and time, and relevant status changes. Audit logs should be searchable/filterable and protected from unauthorized modification or deletion. | **DONE** | `AuditLog.tsx` — captures user/target/action/reason/timestamp for account actions + verification/discount decisions; filterable by type/date/performer; CSV export; read-only. **Login events**: closed by A1's `login_events` table. **Fare changes**: `fare_config` versioned, surfaced via `listFareConfigHistory()`. **2026-09-21 — complaint status history added**: new `complaint_status_history` table (append-only, PSO-read-only RLS) + `trg_log_complaint_status_change` trigger on `public.complaints` (fires on any write path that changes `status` — currently `record_complaint_resolution`, `schedule_complaint_mediation`, and Staff's direct triage `.update()`, all covered automatically since the trigger is table-level, not RPC-specific). Surfaced as a "Status History" section in the Complaints detail modal (old → new status, who, when). Scoped to status transitions only, not a full column-diff, per the panelist's literal ask. `supabase/migrations/20260921000009_add_complaint_status_history.sql` + `20260921000011_revoke_execute_on_log_complaint_status_change.sql` (advisor hygiene — the trigger function showed as PostgREST-exposed even though Postgres blocks calling trigger functions outside trigger context), **applied live**. 2 new service tests. |
| A17 | PSO Users Management | Do not use Base64 as a security mechanism for passwords. Base64 is encoding, not encryption or password hashing. Passwords should be handled using an appropriate secure password-hashing mechanism. Retain the forced password-change requirement for temporary passwords and ensure that temporary credentials expire or become invalid after first use. Panelist question: *"You stated that temporary passwords use Base64. Why did you choose Base64, and do you consider Base64 encryption?"* | ADDRESSED (concern moot) + gap now fixed | `supabase/functions/admin-create-pso-user/index.ts:33-42` — `btoa()` is used **only** to turn 18 cryptographically-random bytes into a printable ASCII string for a one-time temp password value, not as a storage/transmission "encryption" mechanism. That password is passed to `serviceClient.auth.admin.createUser()` — Supabase Auth (GoTrue) bcrypt-hashes it before persisting. No Base64-as-credential-storage found anywhere in a repo-wide grep. **2026-09-21**: the remaining gap (reuse of the temp password as the new password) is now fixed — see A21. |
| A18 | PSO Users Management | Verify that the RBAC implementation enforces the defined permissions of Administrator, PSO Supervisor, and PSO Staff at the backend/API level and not only by hiding interface elements. | EXISTS | `apps/admin/src/lib/rbac.ts`/`RoleGate.tsx` are explicitly documented as UX-only mirrors of DB-side `is_pso()`/`is_supervisor()`/`is_admin()` functions (`SCHEMA.MD:1520-1530`). Confirmed real backend enforcement via `20260915000002_harden_rls_p1_findings.sql`, which fixed actual privilege-escalation bugs (e.g. PSO Staff blocked from PATCHing complaint-resolution columns via trigger even where RLS `USING` alone wouldn't catch it; drivers blocked from self-approving verification fields). Every write action is backed by a `SECURITY DEFINER` RPC with its own independent role check. |
| A19 | Barangay Management | Define the purpose of Barangay Management in relation to the approved geographical scope of TriSakay. Prevent duplicate barangay records and require confirmation before modifying or deactivating a barangay that is already associated with drivers, passengers, or trips. | **DONE (dependency check + edit confirmation + friendly duplicate error)** | `Barangays.tsx`, `packages/services/src/admin/barangays.ts` — duplicate names prevented via the DB `unique` constraint. **2026-09-21**: `createBarangayForAdmin`/`updateBarangayForAdmin` now translate a `23505` violation into "A barangay with that name already exists." instead of the raw Postgres error — same pattern as `submitDriverDocuments`' duplicate-plate handling, chosen over a proactive pre-check to avoid a race condition. **What changed earlier**: Delete's confirmation now shows the *actual* count of `ride_requests` rows referencing that barangay (`countRideRequestsForBarangay()` — the only real FK to this table; drivers/passengers relate to a barangay only indirectly via tricycle cluster, not a foreign key), instead of a static generic sentence. Editing a barangay now shows a confirmation step too, but scoped to when the **cluster** field actually changes — the one edit with real operational impact (changes which tricycles can be matched here) — rather than confirming every notes/name typo fix. 2 new service tests (303 total). No in-app statement of TriSakay's approved geographic scope (ties to S3, unchanged). |
| A20 | System Settings | Restrict system configuration changes to authorized roles and maintain an audit trail containing the previous value, new value, responsible user, and timestamp. For critical settings such as fare-related parameters, require confirmation before changes are applied. Panelist question: *"If an administrator changes the fare from ₱15 to ₱20, where is that change recorded, and can we determine who changed it and when?"* | **PARTIAL → confirmation dialog added** | `SystemSettings.tsx` — "Save changes" now opens a `ConfirmModal` (danger tone, matching the existing "Affects live fares" badge) showing old → new values for all three fields before `saveFareConfig()` is called; modal stays open and shows the error inline if the save fails. Fare changes are genuinely auditable (versioned rows, "Last changed {date} by {name}" shown in UI) and write access is `is_admin()`-gated at both RLS and RPC level — **directly answers the panelist's question**. **Remaining gap, reaffirmed 2026-09-21**: feature toggles (GCash/cash enabled, etc.) are explicitly *not versioned* per code comment — no old/new-value/who/when trail for those, only for the fare matrix. Revisited this round and deliberately left as-is (not reversed): a toggle flip is a live in-place update, not a policy amendment worth preserving history for, per `docs/ADMIN_TODO.MD` open decision #5. |
| A21 | Forced Password Change | Retain the forced password-change mechanism for newly created administrative accounts. Enforce password requirements and prevent reuse of the temporary password as the new password. The temporary credential should become invalid after successful password replacement. | **DONE** | `ForcePasswordChange.tsx` — gated flow exists, enforces password policy (10+ chars, mixed case, number/symbol), clears `must_change_password` on success (old temp password stops working once credential is replaced). **Reuse check added**: `useSessionStore.completePasswordChange()` now re-runs `verifyCurrentPassword(email, newPassword)` — the standard sign-in re-auth check, same mechanism already used by `changeOwnPassword()` — against the CANDIDATE new password before calling `updatePassword()`. If that sign-in succeeds, the candidate matches the still-active temp password, so the change is rejected with an explicit message instead of proceeding. This is the server round-trip `passwordPolicy.ts`'s comment said was needed and previously left undone; no local storage of the temp password's plaintext was required. 1 new test (71 admin tests total). |
| A22 | Admin Dashboard (PSO Supervisor) | Ensure that the PSO Supervisor dashboard displays only information and functions permitted by the defined RBAC policy. Verify through testing that PSO Supervisor accounts cannot access Administrator-only functions by directly entering restricted URLs or invoking protected endpoints. | EXISTS | `App.tsx:69-153` — `RequireAdmin` wraps Admin-only routes (`/pso-users`, `/settings`, `/barangays`); backed additionally by RLS/RPC role checks (same hardening as A18) and the Edge Function's own `callerRow.role !== 'admin'` check — genuine defense-in-depth, not UI-only routing. |

---

## Whole System Comments/Suggestions

| # | Topic | Recommendation | Verified | Notes |
|---|-------|-----------------|--------|-------|
| S1 | "Smart" component | Panelist asks: *"What exactly makes TriSakay smart?"* Should clearly define and demonstrate the "smart-based" component of TriSakay claimed in the thesis title. | Open | Thesis-defense-level concern, not purely technical — no code to verify. |
| S2 | Pre-deployment hardening | Before final deployment, strengthen security and UAT validation, particularly: password handling, role-based access control, audit logging, emergency/SOS handling, complaint tracking, fare transparency, location privacy, and unsuccessful/error scenarios. | PARTIAL | Verified: real hardening work already happened server-side ahead of this panelist review (2026-09-15 RLS audit). Genuine remaining gaps are the specific items flagged above (A1 lockout/session-timeout, A21/A17 temp-password reuse, A3 filters, A13 required resolution note, A20 confirmation dialog, D9 SOS timeout) — not a wholesale absence of security work as the review might imply. |
| S3 | Scope consistency | Ensure consistency between the stated geographical scope of the study and the actual operational coverage of the application. | **DONE** | Ties to A19. **2026-09-21**: `Barangays.tsx` header now states "TriSakay currently operates within General Santos City only" above the barangay list — the underlying data already covers all of the city's barangays (`docs/SCHEMA.MD`'s fare ordinance reference and barangay seed data are both General Santos City-scoped), this was purely a missing in-app statement, not a data gap. |

---

## Summary Counts

Original tally at first verification pass (2026-09-15/16), kept as a historical snapshot — most PARTIAL/MISSING rows above have since moved to DONE/EXISTS through 2026-09-21; read each row's own Verified column for current status, not this section.

- Passenger App: 22 items (2 marked "No recommendation") — 7 EXISTS, 12 PARTIAL, 2 MISSING (P15, P16), 2 N/A
- Driver App: 13 items — 3 EXISTS, 8 PARTIAL, 2 MISSING (D5, D11)
- Admin Portal: 22 items — 6 EXISTS, 11 PARTIAL, 1 mostly-missing (A3), 1 policy-answer-not-gap (A14), 2 N/A (A6, A10)
- Whole System: 3 items — 1 partial (S2), 1 missing (S3), 1 not code-verifiable (S1)

**Headline takeaway:** the codebase is meaningfully more mature than a first read of the panelist's review suggests — most "Open" items already have real, working implementations. The two items flagged by the panelist as most security-critical are largely resolved already: **A17's literal Base64 concern is moot** (Base64 is only used to generate a random printable string, not to store/transmit credentials — Supabase Auth bcrypt-hashes the real password), and **A18/A22's RBAC-at-backend concern is genuinely addressed** (RLS + SECURITY DEFINER RPCs, not just UI hiding). The real open items are narrower and more concrete than the original review framed them — see the "Concrete Real Gaps" list below.

## Concrete Real Gaps (historical — all resolved as of 2026-09-21)

This section tracked the items where the panelist's concern was still valid after code verification. As of 2026-09-21 every one of them is DONE, a documented policy answer, or an explicitly reaffirmed scope decision — see each item's own row above for the current, authoritative status (this list is no longer maintained separately to avoid drifting out of sync with the per-row table again, which happened once already this round).

Only genuinely open item left in the whole doc: **S1** (thesis-framing, "what makes TriSakay smart" — not code-verifiable, a defense-prep question rather than something fixable in code).
