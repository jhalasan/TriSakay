# Admin (PSO) Web App — Build Backlog

**Scope:** `apps/admin` only (plus the shared `packages/*` it needs). Written 2026-08-06 after the full-wireframe UI build (all 11 screens from the wireframe kit, grounded in `docs/CONTEXT.MD` and `docs/SCHEMA.MD`) landed against what was previously the untouched Vite scaffold flagged as the largest remaining scope block in `docs/CHECKLIST.MD:16`.

Cross-reference: `docs/CONTEXT.MD` §6 (RBAC matrix), §7 (Admin Web App feature list), §11 (wireframe review items — MTOP fields, S+ gating, "Confirm cash received"), FR-1.4a/1.5/1.6a (verification), FR-3.10–3.15 (fare discount review), FR-4 (complaint handling), FR-5 (monitoring/reporting), FR-6 (user management), FR-8 (system settings), FR-9.7 (transaction oversight), FR-10.3–10.4 (rating oversight). `docs/DRIVER_TODO.MD` / `docs/PASSENGER_TODO.MD` are the sibling docs and share the same open Edge Function blockers noted below.

---

## Where things actually stand

**All 11 wireframe screens exist and are navigable end-to-end**: Login, Dashboard/Overview, Driver Management, Driver & Tricycle Verification, Passenger Management, Ride Monitoring, Complaints Management, Reports & Analytics, PSO User Management, System Settings, Log out. Typecheck clean, 15 new admin-side tests passing (90/90 repo-wide). Layout is ported from the wireframe exactly (176px sidebar, top bar, wireframe's crossed-box `.ph-box` chart/map placeholders); the palette uses the existing brand tokens from `packages/ui/src/theme/colors.ts` (WCAG-verified navy/green/red) mirrored into `apps/admin/src/styles/tokens.css`, rather than the wireframe's literal grayscale.

**This pass was explicitly frontend-only, by instruction — nothing below is backend-wired:**
- No Supabase client, query, or Auth call exists anywhere in `apps/admin` yet.
- Every screen reads from typed mock fixtures in `apps/admin/src/mocks/*`, routed through a service layer (`apps/admin/src/services/*`) whose function signatures already match the eventual Supabase queries — e.g. `listDrivers()` will become a `.from('users')` join, `suspendDriver()` will become an `account_actions` insert alongside a `users.status` update.
- **F1 is now done (see below)** — `routes/Login.tsx` authenticates for real against Supabase Auth via `useSessionStore`, reads `users.role`, and rejects (signs back out) anything outside `pso_staff`/`pso_supervisor`/`admin`. The dev-only role switcher is gone from the TopBar; RBAC gating is driven by the real signed-in role.
- RBAC gating is fully real and demonstrable at the UI layer (`RoleGate` component, `lib/rbac.ts` mirrors the DB helpers `is_pso()`/`is_supervisor()`/`is_admin()` in `docs/SCHEMA.MD` §7).
- Mock fixtures are schema-shaped (same enum values, column names, and FK relationships as `docs/SCHEMA.MD`) so wiring is a mapper change, not a reshape.

**Database/backend: ready, not a blocker for most items below.** `users`, `driver_profiles`, `tricycles`, `driver_documents`, `passenger_discounts`, `complaints`, `complaint_attachments`, `account_actions`, `fare_config`, `system_settings`, `transactions`, `ratings`, `notifications` all exist with RLS already scoped to `pso_staff`/`pso_supervisor`/`admin` (`docs/SCHEMA.MD` §7). Views `v_overdue_complaints`, `v_expiring_franchises`, `v_flagged_low_ratings`, `v_driver_earnings` already exist and are unread by any app code yet.

---

## Build order (backend wiring — the actual remaining work)

Every item below starts from "screen exists, reads/writes a mock service" rather than "screen doesn't exist." The UI-building step is done for all of them.

| # | Feature | Notes | Schema objects |
|---|---|---|---|
| F1 | ~~**Authentication & session (FR-1.2).**~~ **Done.** `useSessionStore` now calls real `@trisakay/services` `signIn()`/`getSession()`/`getCurrentUserProfile()`/`onAuthStateChange()`; a non-PSO role (`passenger`/`driver`) is signed back out immediately with "This account is not authorized for the admin portal" rather than merely hiding controls. `App.tsx` gained an `isHydrating` guard (so a page refresh with a valid session doesn't flash-redirect to `/login`) and a `RedirectIfAuthed` wrapper on `/login`. TopBar's dev-only role switcher is deleted; it now shows the real signed-in role. New: `apps/admin/src/lib/supabase.ts` (init, called from `main.tsx` — deliberately *not* imported by the store itself, since that file reads Vite-only `import.meta.env` and would break the store's own `node --test` suite), `apps/admin/.env.local`/`.env.example`, `@trisakay/services` added as a real dependency (package.json + vite.config.ts alias + tsconfig reference). 4 new tests in `apps/admin/tests/useSessionStore.test.ts`. | `apps/admin/src/store/useSessionStore.ts`, `apps/admin/src/lib/supabase.ts`, `apps/admin/src/App.tsx`, `apps/admin/src/components/TopBar/TopBar.tsx`, `apps/admin/src/routes/Login.tsx` | `users`, Supabase Auth |
| F2 | ~~**Dashboard (FR-5.5, FR-1.7).**~~ **Done.** New `packages/services/src/admin/dashboard.ts` (first file in a new `packages/services/src/admin/` directory that later F3–F13 items will share) — `getAdminDashboardStats()` runs 4 parallel `count: 'exact', head: true` queries (Total Drivers: `users` where `role='driver'`; Active Rides: `trips` where `status='active'`; Pending Verifications: `driver_profiles` where `verification_status='pending'`; Open Complaints: `complaints` excluding resolved/dismissed) as one all-or-nothing call, plus `listOverdueComplaints()`/`listExpiringFranchises()` reading `v_overdue_complaints`/`v_expiring_franchises` and `listRecentTripActivity()` reading `trips` ordered by `updated_at desc` — all three resolve driver/complainant/accused names via a shared `resolveUserNames()` follow-up `users` lookup (no multi-hop PostgREST embed) with a per-row `null` degrade rather than a whole-call error. `apps/admin/src/services/dashboard.ts` is a thin wrapper matching the app's one-file-per-feature convention. `routes/Dashboard.tsx` now calls only that module — its old dependency on `services/drivers`/`services/monitoring`/`services/verification`/`services/complaints` (those screens' own mock services) is gone. Two new stat tiles ("Overdue Complaints", "Expiring Franchises") and two new `DataTable` panels for the overdue-complaint/expiring-franchise rows, built from existing `StatTile`/`DataTable`/`Badge` components only. "Recent Activity"'s relative-time column now comes from a new `formatRelativeTime()` in `lib/format.ts` over real `updated_at` timestamps. Each of the 4 data sources renders its own independent error text. The two `.ph-box` chart placeholders are unchanged — still blocked on F8's charting-library decision (open decision #4 below). 10 new tests in `packages/services/tests/admin-dashboard.test.ts`. Not yet exercised against a live signed-in PSO session (this worktree has no `.env.local`) — verified via typecheck + unit tests only. | `packages/services/src/admin/dashboard.ts`, `apps/admin/src/services/dashboard.ts`, `apps/admin/src/routes/Dashboard.tsx` | `users`, `trips`, `driver_profiles`, `complaints`, `v_overdue_complaints`, `v_expiring_franchises` |
| F3 | ~~**Driver management (FR-6.1, 6.2).**~~ **Done.** `routes/Drivers.tsx` + `services/drivers.ts` now read real `users` ⋈ `driver_profiles` ⋈ `tricycles` (merged client-side in new `packages/services/src/admin/drivers.ts`, no multi-hop PostgREST embed — same convention as `admin/dashboard.ts`). **Real blocker found and fixed along the way**: `users.status` has no PSO-writable RLS policy at all (only `is_admin()` can `UPDATE public.users` directly) — Flag/Suspend/Reactivate would have 403'd for every PSO Staff/Supervisor account. Added a `perform_account_action()` security-definer RPC (mirrors the `actions_flag_staff` RLS policy's own authorization logic internally, then atomically inserts the `account_actions` audit row + updates `users.status` — same idiom as the driver app's `complete_trip`/`cancel_trip`). `account_actions.reason` is `NOT NULL` with no UI to collect one previously — `ConfirmModal` gained an optional `reasonRequired` textarea + `confirmLoading`, and Flag/Suspend/Reactivate now route through it. A driver with no tricycle registered yet (confirmed against real data — 2 of 3 real driver rows have none) no longer breaks the row; `DriverRow.cluster` widened to allow `null`. Live-tested the RPC directly against the real project in rolled-back transactions (authorized success, unauthenticated caller, wrong-role caller, empty-reason caller) — a `coalesce(is_pso(), false)`/`coalesce(is_supervisor(), false)` fix was needed after the first pass showed a NULL-role caller could silently bypass the authorization check (PL/pgSQL's `IF NOT <NULL>` doesn't raise). New tests: `packages/services/tests/admin-drivers.test.ts`, `admin-accounts.test.ts`; `apps/admin/tests/services.test.ts` driver tests rewritten against a fake Supabase client instead of the deleted mock array. | `users`, `driver_profiles`, `tricycles`, `account_actions` |
| F4 | ~~**Driver & tricycle verification (FR-1.4a, 1.5).**~~ **Done. Implemented 2026-08-15, migrations applied and live-verified 2026-08-17.** `routes/DriverVerification.tsx` + new `packages/services/src/admin/verification.ts` (`listPendingVerifications()`, `updateVerificationFields()`, `approveVerification()`/`rejectVerification()`) read/write real `driver_profiles` ⋈ `tricycles` ⋈ `driver_documents` (merged client-side, same convention as F3/F5). **Two real, more severe bugs found upstream while wiring this, both blocking driver onboarding entirely, not just this screen**: (1) `driver_documents`'s own `doc_target_consistency` check constraint requires a non-null `tricycle_id` for every document except the license, but `submitDriverDocuments()` never had one to give it — no `tricycles` row exists at registration by design, so every non-license document insert should have been rejected, aborting the whole batch (the license document too, since all 4 rows are one INSERT). (2) Nothing anywhere ever moved `driver_profiles.verification_status` from `unsubmitted` to `pending` — no trigger exists, and RLS explicitly blocks a driver from setting it themselves (`driver_update_self`'s `with check` requires the value stay unchanged) — so a case could never have entered this screen's queue even if (1) were fixed in isolation. Fixed both with one new security-definer RPC, `submit_driver_documents(p_plate_no, p_documents)`, mirroring the `perform_account_action`/`complete_trip` idiom: creates the driver's `tricycles` row from a plate number the registration form now collects (`apps/driver/app/(auth)/register.tsx` gained a "Tricycle plate number" field — the only vehicle-identifying data the form has ever collected, matching the wireframe's plate number already being known at review time), attaches all 4 documents to it with the correct `tricycle_id` per the check constraint, and flips both `verification_status` fields to `pending` — all in one transaction (idempotent via `ON CONFLICT`, so an accidental double-submit updates in place instead of erroring). Approve/Reject go through a second new RPC, `perform_verification_decision(p_driver_id, p_decision, p_notes)`, atomically updating `driver_profiles`, the active `tricycles` row, and every one of that driver's `driver_documents` rows together (status + `verified_by`/`reviewed_by` + timestamps + a shared `remarks` from the Notes field — there's no dedicated notes column anywhere, so PSO's decision note is written to every document's `remarks` instead). `listPendingVerifications()` deliberately includes `approved`/`rejected` cases too (not just `pending`) — the case-list `Badge` already rendered all three tones, and an existing test (`apps/admin/tests/services.test.ts`, predating this pass) already assumed a decided case stays visible with its updated status, so this pass matched that established intent rather than the wireframe panel's literal "Pending Cases" heading. Also fixed a latent RLS/UI mismatch while here: the MTOP/cluster/notes fields were editable by PSO Staff in the UI but `tricycles_owner_rw`'s RLS `with check` only allows a non-owner write from `is_supervisor()` — PSO Staff would have hit a silent RLS 403 typing into a field they could see. Now the whole transcription-and-decision panel is behind the same `RoleGate min="supervisor"` as the buttons, with a read-only fallback view for PSO Staff. Reject now requires a note (client-side only, mirrors F10's discount-review pattern) and passes it through — the pre-existing wireframe code never actually sent the typed Notes text to either action. Document images remain `PlaceholderBox`es, matching F10's precedent (no signed-URL rendering yet). New/updated tests: `packages/services/tests/{driver-documents,admin-verification}.test.ts`, `apps/admin/tests/services.test.ts`'s verification block rewritten against a stateful fake client. Full repo test suite green, typecheck clean. **Live-verified end-to-end against the real project 2026-08-17**: a real throwaway driver uploaded 4 real files to `driver-docs` and called `submit_driver_documents` — confirmed the tricycle, all 4 documents (correct `tricycle_id` split), and both `verification_status` fields landed exactly as designed, including a resubmit-after-reject pass that updated the same 4 rows in place rather than erroring or duplicating; a real throwaway PSO Supervisor account saw the case via the same RLS-scoped query this screen uses and approved it via `perform_verification_decision`, confirmed `verified_by`/`reviewed_by`/timestamps/shared remarks landed everywhere; a non-supervisor and an unauthenticated caller were both correctly rejected by the RPC's own auth check. All test data cleaned up (0 leftover DB rows) except 8 tiny dummy files in `driver-docs` that couldn't be removed — see the storage-bucket-delete-policy gap noted in `docs/CHECKLIST.MD`, found during this cleanup. FR-1.6a's in-app + email notification to the driver on decision is still explicitly out of scope (no email infrastructure exists in the repo). | `driver_profiles`, `tricycles`, `driver_documents` |
| F5 | ~~**Passenger management (FR-6.1, 6.2).**~~ **Done.** `routes/Passengers.tsx` + `services/passengers.ts` now read real `users` + a completed-ride count (`ride_requests`) + an approved-discount flag (`passenger_discounts`), merged client-side in new `packages/services/src/admin/passengers.ts` (PostgREST has no `GROUP BY`, so the ride count reduces client-side — fine at this app's pilot-barangay scale). Block/Unblock go through the same `perform_account_action()` RPC and reason-required `ConfirmModal` added for F3 (Block → `suspend`, Unblock → `reactivate`, preserving the existing wireframe-label mapping). New tests: `packages/services/tests/admin-passengers.test.ts`; `apps/admin/tests/services.test.ts` passenger tests rewritten the same way as F3's. | `users`, `account_actions` |
| F6 | ~~**Ride monitoring (FR-5.1, 5.2, NFR-2.5).**~~ **Done (data only).** New `packages/services/src/admin/monitoring.ts` — `listActiveTricyclesForAdmin()` reads real `driver_profiles` (`is_available=true` = on the clock), `tricycles` (plate/capacity), `trips` (`status='active'` = the driver's current trip), and `ride_requests` (seats taken, summed per trip) — merged client-side, same convention as every other admin module. A driver with no active trip shows `idle` with 0 seats taken but their tricycle's full capacity; a driver with no tricycle row at all degrades to plate `'—'`/0 seats rather than being dropped. Deliberately a one-shot fetch, not a Realtime subscription — matches the architecture of every other admin screen (Dashboard included), and the map placeholder stays a placeholder per the user's explicit choice when this was scoped (map-library decision is still cross-app and unresolved). Also removed genuinely dead code found along the way: `listRecentActivity()`/`RecentActivityRow` (`apps/admin/src/services/monitoring.ts`, `types/ride.ts`) were leftover from before F2 rewired the Dashboard to its own module — nothing called them anymore. `apps/admin/src/mocks/rides.ts` deleted. New tests in `packages/services/tests/admin-monitoring.test.ts`. | `driver_profiles`, `tricycles`, `trips`, `ride_requests` |
| F7 | ~~**Complaints (FR-4.3, 4.3a).**~~ **Done (triage + DH directive only).** New `packages/services/src/admin/complaints.ts` — real `complaints` reads (with requester/accused names resolved via the usual follow-up `users` lookup) and writes for the two flows the screen already had UI for: PSO Staff triage (`setComplaintStatusForAdmin` stamps `triaged_by`/`triaged_at`) and the Department Head directive (`recordDhDirectiveForAdmin` stamps `dh_reviewed_by`/`dh_reviewed_at`). `businessDaysElapsed` is computed client-side (`apps/admin/src/lib/format.ts`'s new `businessDaysSince()`) rather than one RPC call per row — it deliberately reproduces `business_days_since()`'s exact weekday-counting logic so the number matches `v_overdue_complaints` for the same complaint. **Removed a `priority` filter/field that only ever existed in the mock** — no `complaints` column backs it and no FR anywhere in `docs/CONTEXT.MD` mentions it; kept it would have meant a filter faking its own data, so it's gone rather than wired. **Still open, genuinely new screen work, not done here**: mediation scheduling (date/time + location, FR-4.5) and outcome recording (FR-4.6) have no UI yet; complaint evidence via `complaint_attachments` is unwired. `apps/admin/src/mocks/complaints.ts` deleted. New tests in `packages/services/tests/admin-complaints.test.ts`. | `complaints` |
| F8 | ~~**Reports & analytics (FR-5.3, 5.4, 9.7).**~~ **Done (numbers/table/export; charts stay placeholders per user decision).** New `packages/services/src/admin/reports.ts` — `getAdminReportSummary(sinceIso)` counts completed `ride_requests` and sums `paid` `transactions` in the window, and buckets ride times into 2-hour windows (local wall-clock, matching the rest of the app's `en-PH` rendering) to find the peak; `listTransactionsForAdmin(sinceIso)` resolves passenger/driver names through the 3-hop `transactions → ride_requests → trips → users` chain, degrading a cancelled-before-assignment transaction's `driverName` to `'—'` rather than dropping the row. The "Date range" selector now actually filters both queries (`apps/admin/src/services/reports.ts`'s `dateRangeSinceIso()`); "Export CSV" now really exports the loaded transaction list (new `apps/admin/src/lib/csv.ts`, plain-`<a download>`, no new dependency). **Removed the "Report type" selector** — same reasoning as `priority` above: it never filtered anything even against the mock, and there's no real distinct dataset behind "Complaints"/"Driver activity" to switch to without designing new report views, which is out of scope for a wiring pass. Charts remain `.ph-box` placeholders — no charting library is installed, by explicit user decision when this was scoped. New tests in `packages/services/tests/admin-reports.test.ts`. | `ride_requests`, `transactions` |
| F9 | ~~**Rating oversight (FR-10.3, 10.4).**~~ **Done.** New route `routes/RatingOversight.tsx` (own sidebar entry, `/rating-oversight`) — a read-only `DataTable` over `v_flagged_low_ratings` (`rating_count >= 5` and `rating_avg` below `system_settings.low_rating_threshold`, both already computed by the view). New `packages/services/src/admin/ratings.ts` (`listFlaggedLowRatings()`) + thin `apps/admin/src/services/ratings.ts` wrapper, `useRatingOversightStore`. No write path — FR-10.3/10.4 don't define an oversight *action* beyond surfacing the list; a PSO acting on a flagged driver does so via Driver Management's existing Flag/Suspend. 2 new tests in `packages/services/tests/admin-ratings.test.ts`. | `v_flagged_low_ratings`, `ratings` |
| F10 | ~~**Fare discount review (FR-3.10–3.15, UC48).**~~ **Done.** New route `routes/DiscountReview.tsx` (own sidebar entry, `/discounts`), mirroring `DriverVerification.tsx`'s case-list + review-panel layout rather than a plain table, since this is also an S+-gated document review — front/back ID photo panels are `PlaceholderBox`es (no image rendering yet, same as `DocumentPanel` on the Verification screen). Approve/Reject write `passenger_discounts.status`/`reviewed_by`/`reviewed_at`/`remarks` directly under the existing `discounts_review_supervisor` RLS policy — no new RPC needed here, unlike Driver/Passenger Management's `perform_account_action` situation, since this table's own RLS already scopes the write correctly. Reject requires a remarks note (client-side only — the DB column itself doesn't enforce this); Approve's remarks are optional. New `packages/services/src/admin/discounts.ts` (`listPendingDiscounts()`, `approveDiscount()`, `rejectDiscount()`) + `apps/admin/src/services/discounts.ts` wrapper, `useDiscountsStore`. 5 new tests in `packages/services/tests/admin-discounts.test.ts`. **Minor pre-existing gap noted, not fixed here**: `discounts_review_supervisor`'s RLS `with_check` doesn't constrain `reviewed_by` to `auth.uid()` (unlike `account_actions`'s `performed_by` check) — any supervisor's client could technically misattribute review authorship. Low severity (`is_supervisor()` still correctly gates *who* can approve/reject), flagging for awareness rather than blocking on it. **Not live-UI-tested** (no real `passenger_discounts` rows exist in this project yet to review against) — verified via typecheck, full test suite, and a production build only. | `passenger_discounts` |
| F11 | ~~**PSO user management (FR-6.3).**~~ **Done.** New `packages/services/src/admin/psoUsers.ts` — `listPsoUsersForAdmin()` reads real `users` scoped to `role in ('pso_staff','pso_supervisor','admin')`. "Add PSO user" needed a real create flow with no email/invite infra in the repo (open decision #3, now resolved) — a browser client can never safely hold a service-role key, so account creation had to move server-side: new Edge Function `supabase/functions/admin-create-pso-user` (`verify_jwt: true`, deployed) verifies the caller is a real `admin` via their own JWT, then uses the service-role client to call `auth.admin.createUser()` with `email_confirm: true` and a generated one-time temp password, returned directly in the response (never emailed, never logged/stored beyond that response) for the admin UI to display once (`apps/admin/src/routes/PsoUsers.tsx`'s new "Account created" panel, with copy-to-clipboard). One correctness trap found and handled: `handle_new_auth_user()` (the P0 anti-privilege-escalation fix from earlier this session) unconditionally downgrades any non-`driver` metadata role to `passenger` — that guard fires for this Edge Function's `createUser()` call too, since it's the same DB trigger regardless of caller. So the real PSO role is set in a second step after creation, via the service-role client (bypasses RLS legitimately, server-side only); a failed role-assignment step deletes the just-created auth user rather than leaving an orphaned `passenger` account behind. Disable/Enable now goes through the same `perform_account_action` RPC and reason-required `ConfirmModal` as Driver/Passenger Management (F3/F5) — the RPC doesn't restrict by target role, so no changes needed there; converted the screen from local `useState` to a proper `usePsoUsersStore.ts` (zustand), matching every other S+-gated screen's pattern. **Live-verified end-to-end against the real project**: a real throwaway Administrator account called the real deployed Edge Function, the new account's role landed as `pso_staff` (not downgraded), and — the real proof, not just "no error" — the new account successfully signed in using the returned temp password. Disable/re-enable via `perform_account_action` verified the same way. All test data cleaned up (0 leftover rows). New tests in `packages/services/tests/admin-psoUsers.test.ts`. | `users`, Supabase Auth Admin API |
| F12 | ~~**System settings (FR-8.1).**~~ **Done.** New `packages/services/src/admin/settings.ts`. `fare_config` is versioned (`fare_config_one_active` partial unique index enforces exactly one `is_active=true` row) — real production passengers get fare quotes from that live row via `compute_fare()`/`getFareConfig()` (`packages/services/src/fare/index.ts`), so an update can never leave zero active rows even momentarily. Two separate client-side UPDATE/INSERT calls can't guarantee that; a new migration adds `update_fare_config()` (security-definer RPC, `coalesce(is_admin(), false)` guard) that deactivates the old row and inserts the new active one in one atomic call — the UI only ever edits `baseFare`/`baseKm`/`ratePerKm`, so `discountRatePercent`/`ordinanceRef` carry over from the current active row untouched. **Feature toggles (open decision #5) resolved**: a small additive migration added `gcash_enabled`/`cash_enabled`/`franchise_expiry_notifications` boolean columns directly to `system_settings` (already RLS-gated `is_admin()` ALL, no new policy needed) rather than a new table — toggles are live in-place updates, not versioned history worth preserving the way a fare-ordinance amendment is. The "matching heuristic" thresholds stay read-only in the UI (no edit form existed for them before this pass either). **Live-verified with care, given fare_config feeds real production fare quotes**: rather than writing to the live row directly, verified both the RPC and the toggle write via rolled-back transactions (`begin; set local role authenticated; select set_config('request.jwt.claim.sub', ...); ...; rollback;`) against the real project — confirmed the admin path atomically deactivates the old row and creates a correctly-valued new one, confirmed a non-admin caller is cleanly rejected by both the RPC's own check and system_settings' RLS, and confirmed the live table was byte-for-byte unchanged after each rollback. New tests in `packages/services/tests/admin-settings.test.ts`. | `fare_config`, `system_settings` |
| F13 | **Transaction oversight (FR-9.7).** Folded into Reports — `listTransactionsForAdmin()` (F8) is now real, date-ranged, and resolves passenger/driver names. Still open: whether PSO Staff+ needs a dedicated transactions view/filter set beyond what Reports already covers; not decided, not blocking. | `transactions` |
| F14 | **Deployment (CONTEXT §3, P5 in `docs/CHECKLIST.MD`).** Vercel is the named target; `apps/admin` has no `vercel.json` yet. Needs a SPA rewrite rule (all routes → `index.html`) since `react-router-dom`'s `BrowserRouter` is history-based, plus `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` env vars once F1 lands. | config |

---

## Open decisions / cross-app blockers (flagged, not resolved)

1. ~~No admin-role service surface exists in `packages/services` yet.~~ **Done.** `packages/services/src/admin/` now covers every screen: `dashboard.ts` (F2), `drivers.ts`/`passengers.ts`/`accounts.ts` (F3/F5), `verification.ts` (F4), `monitoring.ts` (F6), `complaints.ts` (F7), `reports.ts` (F8), `psoUsers.ts` (F11), `settings.ts` (F12), `ratings.ts`/`discounts.ts` (F9/F10).
2. ~~`match-ride-request` is deployed; `paymongo-webhook` is not~~ — **both are now deployed and verified live** (2026-08-07, see `docs/CHECKLIST.MD` P0). F8/F13 can now expect real `transactions.status='paid'` rows to exist once a passenger actually completes a GCash payment — no longer a blocker for those items.
3. **No email-sending infrastructure exists anywhere in the repo** (no Resend/SendGrid/SMTP integration) — still blocks F4's FR-1.6a driver notification. **F11's PSO account creation is resolved without it**: a new `admin-create-pso-user` Edge Function creates the account directly with a one-time temp password shown on screen, no email step in the loop.
4. **Charting library choice is unmade.** F8 needs one; nothing is installed repo-wide. Deliberately still deferred — F8's numbers/table/export are real now, but the two chart placeholders stay `.ph-box` until a library is chosen on purpose, not as a side effect of a wiring pass.
5. ~~Feature-toggle persistence (F12) has no target table.~~ **Resolved.** Added `gcash_enabled`/`cash_enabled`/`franchise_expiry_notifications` boolean columns directly to `system_settings` (already `is_admin()`-gated) rather than a new table.
6. **`react-router-dom@7` was added as a new `apps/admin` dependency** this pass (previously absent repo-wide) — the only new production dependency introduced. `zustand` was also declared explicitly in `apps/admin/package.json` (already hoisted repo-wide via the Driver/Passenger apps, just not previously listed as an admin dependency).
