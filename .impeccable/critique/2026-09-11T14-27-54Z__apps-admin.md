---
target: apps/admin
total_score: 32.5
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 1
target_identity: "file:D:\\Projects\\TRISAKAY_APP\\apps\\admin"
timestamp: 2026-09-11T14-27-54Z
slug: apps-admin
---
Method: dual-agent (A: ac64a4afd860a4587 · B: aaf9a0cd6fadafe6a)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3.5 | Dashboard attention-card counts use bare "—" during loading instead of skeletons |
| 2 | Match System / Real World | 4 | Unchanged — MTOP/OSCA/ARTA/cluster vocabulary throughout |
| 3 | User Control and Freedom | 3 | ConfirmModal Cancel/Escape everywhere on decision path; no undo once Approved/Rejected |
| 4 | Consistency and Standards | 3.5 | Shared review-workbench classes verified; docked for unexplained "S+" convention |
| 5 | Error Prevention | 3.5 | Reject gated on notes; P0 confirm-step now in front of every S+ decision |
| 6 | Recognition Rather Than Recall | 3 | "S+" superscript and severe icon both require prior knowledge of the convention |
| 7 | Flexibility and Efficiency | 3.5 | "/" shortcut verified working; no keyboard path for Approve/Reject itself |
| 8 | Aesthetic and Minimalist Design | 3.5 | Unchanged — real type-scale progression, restrained |
| 9 | Error Recovery | 3 | ErrorBanner stays inside still-open modal on failure (verified) |
| 10 | Help and Documentation | 2 | No legend anywhere for "S+" or the severe icon |
| **Total** | | **32.5/40** | **Good** |

## Verdict on the 4 Previously-Claimed Fixes

P0 (ConfirmModal on Approve/Reject): HOLDS.
P1 (auto-advance to next pending case): HOLDS — reads store directly to avoid stale-closure re-open bug.
P2 (status select restricted to triage states): HOLDS.
P3 ("/" search shortcut, guarded, hint chip): HOLDS.

## Design Specificity Verdict

Unchanged, still holds — genuinely authored for a PH municipal PSO tool. Deterministic scan: same single false-positive finding as before, broken-image at apps/admin/src/components/LiveMap/LiveMap.tsx:36 (doc comment, not a real empty-src tag). Browser inspection unavailable (no Supabase credentials).

## What's Working

1. P1 auto-advance reads the store directly (getState()) specifically to avoid re-opening an already-decided case — a correctness-aware fix, not surface-level.
2. SevereIcon scoping is precise — Suspend/Block/Disable only, deliberately not Revoke session.
3. Confirm-modal copy names real consequences ("They'll need to resubmit documents before they can go online"), not generic "Are you sure?".

## Priority Issues

[P1] Complaints' case panel always renders Schedule Mediation even when irrelevant — a Supervisor triaging a fresh "open" complaint scans past a disabled form section on every case; inconsistent with Record Outcome, which stays hidden until its precondition is met. Fix: hide Schedule Mediation entirely until canScheduleMediation is true.

[P2] The "S+" superscript is unexplained anywhere in the UI — 13 call sites, no tooltip/aria-label, redundant since RoleGate already hides the button from anyone who can't use it. Fix: add title/aria-label or drop it.

[P2] DataTable's row-level keyboard focus is now redundant with the Actions-column button — a new issue created by combining the earlier role="button" removal with the pre-existing per-row action button; a keyboard user now hits row, checkbox, and a duplicate "Review" button. Fix: drop one of the two redundant controls.

[P3] Bulk triage confirm doesn't show which rows are affected — "Mark 5 selected complaint(s)..." with no way to sanity-check which 5. Fix: list selected subjects (truncated) in the modal.

[P3] Dashboard's attention-card counts skip the app's own skeleton pattern — bare "—" while the table below uses real skeleton bars. Fix: reuse skeleton-bar treatment.

## Persona Red Flags

Alex: auto-advance helps, but Approve/Reject (highest-frequency action) has no keyboard shortcut; bulk triage only selects current page, no "select all N matching filter."

Sam: focus trap correctly implemented (verified). New gaps: accordion buttons have aria-expanded but no aria-controls; DataTable redundant-tab-stop issue hits her harder than Alex.

## Minor Observations

- Frozen-status footnote text quality differs between its two branches.
- ConfirmModal's reasonRequired prop unused by the P0 fix (captures notes via a separate field instead) — two patterns for one need.
- The "which rows selected" gap in bulk-confirm also applies to Drivers.tsx and Passengers.tsx's existing bulk actions, not just Complaints.
